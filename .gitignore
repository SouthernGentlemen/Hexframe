node_modules/
dist/
.wrangler/
*.tsbuildinfo
.DS_Store

# Credentials. None of these may ever be committed: the repository-root .env is the
# local source of truth and the deploy script copies its admin values into production.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
