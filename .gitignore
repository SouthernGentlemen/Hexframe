node_modules/
dist/
.wrangler/
*.tsbuildinfo
.DS_Store

# Credentials. None of these may ever be committed: the lab password and the session
# signing key live in .dev.vars locally and in Cloudflare secrets in production.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
